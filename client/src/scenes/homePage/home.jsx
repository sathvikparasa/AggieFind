const { Box } = require("@mui/material")
const { default: Navbar } = require("scenes/navbar/nav")

const HomePage = () =>
{
    return (
    <Box>
        <Navbar />
    </Box>
    )
}

export default HomePage;